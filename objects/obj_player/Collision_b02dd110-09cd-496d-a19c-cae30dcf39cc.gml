/// @description Insert description here
// You can write your code in this editor

/*
var touching = position_meeting(x, y, obj_bullet);

if (touching.team == 2) {
	hhealth--;
	with (touching) {
		instance_destroy();
	}
}